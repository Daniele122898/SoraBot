using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;
using Discord.Commands;
using Sora_Bot_1.SoraBot.Services;

namespace Sora_Bot_1.SoraBot.Modules.Reminders
{
    public class ReminderModule : ModuleBase
    {
     /*   private ReminderService service;

        public ReminderModule(ReminderService remService)
        {
            service = remService;
        }

        [Command("rem"), Summary("Work in Progress")]
        public async Task Remind(string reminder)
        {
            await service.SetReminder(Context, reminder);
        }
        */
    }
}
